# Local reconstruction recordings

The v4.13 data-recorder launcher creates one timestamped directory here for
each play session. These generated sessions can contain ROM-derived framebuffer,
VRAM, geometry, palette, sprite, and emulation-state data. They are deliberately
ignored by Git and must not be added to releases or commits.

Keep sessions locally as reference material for future course, object, texture,
and sprite reconstruction work.
